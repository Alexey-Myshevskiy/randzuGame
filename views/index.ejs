<!DOCTYPE html>
<html>
<head>
    <title>Новый игрок</title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css'/>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
</head>
<body>
<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">Для начала игры введите имя</h3>
    </div>
    <div class="panel-body">
        <form action="/players-room" method="post">
            <div class="form-group">
                <label for="pName">Отображаемое имя в игре:</label>
                <input type="text" class="form-control" id="pName" name="playerName" required>
            </div>
        </form>
        <button id="submit" class="btn btn-success">Подключиться</button>
    </div>
    <div class="panel-footer">
        <div class="row">
            <div class="col-lg-2 col-md-2 col-sm-3">
                <p>Количество игроков: <span class="badge"><%= pCount || 0 %></span></p>
            </div>
            <div id="progress"
                 class="col-lg-3 col-md-3 col-sm-4 col-md-offset-7 col-lg-offset-7 col-sm-offset-5"
                 style="visibility: hidden">
                <strong>Проверка имени...</strong>
                <div class="progress progress-striped active nameIndicator">
                    <div class="progress-bar" role="progressbar" aria-valuenow="100" aria-valuemin="0"
                         aria-valuemax="100"
                         style="width: 100%">
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script>
    $("#submit").click(function () {
        var workDiv = $("#progress");
        workDiv.css("visibility", "visible");
        var value = $("#pName").val();
        $.ajax({
            url: "http://localhost:3000/checkName?name=" + value,
            success: function (result) {
                if (!result.isExist) {
                    $("form").submit();
                }
                else {
                    workDiv.html("<div class='alert alert-warning' role='alert'>" +
                            "<strong>Это имя уже занято, выберете другое</strong></div>");
                    $("input").val('').focus();
                }
            },
            error: function (err) {
                console.log(err);
            }
        });
    });
</script>
</body>
</html>
